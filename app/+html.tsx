/**
 * @file app/+html.tsx
 * @description Root HTML template for web
 * This file helps Expo Router understand the app structure
 */

export default function Root({ children }: { children: React.ReactNode }) {
  return children;
}
